apiVersion: v1
kind: Service
metadata:
  name: college-event-service

spec:
  selector:
    app: college-event-website

  type: NodePort

  ports:
  - protocol: TCP
    port: 8086
    targetPort: 8086
    nodePort: 30086