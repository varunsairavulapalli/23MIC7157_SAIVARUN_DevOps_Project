define host {
    use                     linux-server
    host_name               college-event
    alias                   College Event Website
    address                 host.docker.internal
}

define service {
    use                     generic-service
    host_name               college-event
    service_description     HTTP
    check_command           check_http
}